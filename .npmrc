
# Install optional dependencies but ignore platform mismatches
optional=true
# Ignore platform checks for optional dependencies
optionalDependencies=true
# Force install even with platform mismatches
force=true